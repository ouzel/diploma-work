<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.RecycleApp" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Основные цвета -->
        <item name="colorPrimary">@color/primary_variant</item>
        <item name="colorPrimaryVariant">@color/black</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Второстепенные цвета -->
        <item name="colorSecondary">@color/secondary_variant</item>
        <item name="colorSecondaryVariant">@color/black</item>
        <item name="colorOnSecondary">@color/white</item>

        <!-- Цвет фона -->
        <item name="android:windowBackground">@color/black</item>

        <!-- Цвет статус-бара -->
        <item name="android:statusBarColor">@color/primary_variant</item>

        <!-- Цвет кнопок -->
        <item name="android:colorButtonNormal">@color/primary_variant</item>
    </style>
</resources>
